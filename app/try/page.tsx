import { GuestTryApp } from "@/components/try/GuestTryApp"

export const metadata = {
  title: "Try as guest | Archivolt",
  description: "Generate one free VBD architecture blueprint without signing up.",
}

export default function TryPage() {
  return <GuestTryApp />
}
